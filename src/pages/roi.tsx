import { Header } from '../app/components/Header';

// get rabbits prices by level
// get crystal prices by level
// get ELE prices
export default function RoiPage() {
    return (
        <>
            <Header />
            <h2>Roi Page</h2>
            <p>
                Waiting for collections to get verified on Magic Eden so we can query the API. Or someone shares their
                Tensor API access, if they have one.
            </p>
        </>
    );
}
